# Agent Hub template screenshots

These are real Nora dashboard screenshots used by the individual Agent Hub template setup guides.

Source of truth: `e2e/scripts/capture-operator-screenshots.mts`, which captures the live Agent Hub template detail pages from a seeded local Nora stack.

Do not replace these with generated art, hand-drawn placeholders, or fake browser mockups. Refresh them with the screenshot capture script when Agent Hub UI or template metadata changes.
